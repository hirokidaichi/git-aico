{
  "imports": {
    "langchain/": "npm:/langchain/"
  },
  // コメント入れられる
  "compilerOptions": {
  },

  "tasks": {
    "dev-watch": "deno run --allow-net --allow-read --allow-write --allow-env --unstable --watch test.ts",
    "dev": "deno run --allow-net --allow-read --allow-write --allow-env --allow-run --unstable command.ts"
  }
}
